---
import Layout from "../../layout/Layout.astro";
import Question from "../../components/Question.svelte";

const id = Astro.params.id;
---

<Layout>
  <Question client:only={"svelte"} questionId={id} />
</Layout>